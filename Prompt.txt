
You are a senior PHP/Laravel architect with 80+ years of experience, building enterprise-grade, maintainable, and scalable Laravel applications.

I will provide a Laravel migration file, and you must recreate the migration if needed and generate all related code based strictly on the migration.

1. Directory & Naming Conventions.In the Code,DO NOT THRUGH EXCEPTIONS when they occur,gracefully handle them and throw them as API responses to the frontend client since we are building restful API. In the request files,provide API responses for error back to the frontend client. Provide industry grade comments on logic for easy understanding.Cover all operations that must be performed on the entity.All file must have single responsibily and should be able to work seamlessly with one another.Use best practices in laravel and follow modern approaches.DO NOT THROW EXCEPTIONS TO AVOID CRUSHING THE API,exceptions should be handled meaningfully and used returned as API responses when necessary.If exceptions are very sensitive,simply log them.FORM REQUESTS SHOULD meaningfully RETURN API VALIDATION ERRORS IN FRIENDLY WAYS TO THE API CLIENT.

All generated files must follow this modular structure:
app/
├── Models/
│   └── [EntityName].php
│
├── Services/
│   └── [EntityName]/
│       ├── [EntityName]Service.php
│       └── Contracts/
│           └── [EntityName]ServiceInterface.php
│
├── Repositories/
│   └── [EntityName]/
│       ├── [EntityName]Repository.php
│       └── Contracts/
│           └── [EntityName]RepositoryInterface.php
│
├── Http/
│   ├── Controllers/Api/
│   │   └── [EntityName]Controller.php
│   │
│   ├── Requests/[EntityName]/ 
│   │   ├── Store[EntityName]Request.php
│   │   └── Update[EntityName]Request.php
│   │
│   └── Resources/
│       ├── [EntityName]Resource.php
│       └── Related[OtherEntity]Resource.php
│
├── Policies/
│   └── [EntityName]Policy.php
│
├── Providers/
│   └── [EntityName]ServiceProvider.php
├── Tests/
│   ├── Feature/[EntityName]ControllerTest.php
│   └── Unit/[EntityName]ServiceTest.php
database/
└── migrations/
    └── create_[entity_name]_table.php


Ensures easy imports, modularity, scalability, and testability.

2. Artisan Commands

First, provide all Artisan commands to generate:

Migration: php artisan make:migration create_[entity_name]_table --create=[entity_name]

Model: php artisan make:model [EntityName]

Controller: php artisan make:controller Api/[EntityName]Controller --api

Policy: php artisan make:policy [EntityName]Policy --model=[EntityName]

Form Requests:

php artisan make:request [EntityName]/Store[EntityName]Request

php artisan make:request [EntityName]/Update[EntityName]Request

Service Provider: php artisan make:provider [EntityName]ServiceProvider

Test scaffolds (optional --unit or --feature)

Include exact file paths for each generated file.

3. Migration Recreation

Recreate the migration according to the provided schema.

Include proper column types, indexes, foreign keys, and constraints.

Use Laravel conventions (timestamps, soft deletes if needed).

Ensure the migration matches the structure required for Service, Repository, and Model logic.

4. Service Provider

Bind Repository and Service interfaces to implementations.

Enable constructor dependency injection and module extraction.

$this->app->bind(
    \App\Repositories\Contracts\[EntityName]RepositoryInterface::class,
    \App\Repositories\[EntityName]\[EntityName]Repository::class
);
$this->app->bind(
    \App\Services\Contracts\[EntityName]ServiceInterface::class,
    \App\Services\[EntityName]\[EntityName]Service::class
);


Register in config/app.php.

5. Layers & Responsibilities
Layer	Responsibility
Controller	Thin orchestration layer; calls Service methods; uses Form Requests and Resources
Service	All business logic; coordinates Repositories; handles transactions
Repository	Handles database interactions only; no business logic
Form Requests	Validation and authorization of incoming requests
Policy	Authorization rules for sensitive actions
API Resource	Format and transform JSON responses; use nested/related resources when returning multiple pieces of info
Tests	Unit tests for Services; Feature tests for Controllers and API responses

Nested Resources: When a model has relationships (belongsTo, hasMany), use additional API Resources instead of embedding raw arrays.

6. Form Requests

Generate Store[EntityName]Request.php and Update[EntityName]Request.php.

Must handle:

Validation rules

Authorization ($this->authorize(...))

Pass $request->validated() to Services to keep Controllers thin.

7. API Routes

Use routes/api.php.

RESTful routes: index, show, store, update, destroy.

Apply middleware and policies.

8. Testing Requirements

Unit Tests (Service Layer):

Test all public Service methods.

Mock Repositories to isolate Service logic.

Verify business rules, transactions, and error handling.

Feature Tests (Controller/API):

Test all endpoints: index, show, store, update, destroy.

Verify authorization using Policies.

Check nested resources and relationships.

Form Request Tests (optional):

Validate request rules for create/update operations.

Verify authorization logic.

9. Code Quality Standards

PHP 8+ strict typing

Constructor-based dependency injection

Explicit return types

Production-grade PHPDoc for classes and methods

Handle edge cases and errors gracefully

Follow Laravel best practices (latest stable version)

Use nested resources for related data

Include Unit and Feature tests for all layers

No invented fields outside the migration

10. Deliverables

Artisan commands

Recreated migration file

Full implementation of all files

Service Provider bindings

API routes

Unit & Feature tests

Brief explanation of architectural decisions

11. Constraints

Do NOT skip any layer

Do NOT invent fields

Assume real-world production usage

Code must be maintainable by large teams

Follow Clean Architecture / Layered Architecture principles

✅ Outcome

Controllers remain thin

Services contain business logic

Repositories handle database interactions

Policies protect actions

Form Requests handle validation and authorization

Resources return consistent API responses and use nested resources for related data

Unit and Feature tests ensure robustness and correctness

Migration fully reflects the entity schema
   /**
         * PATIENTS - Medical identity and health profile
         * Shard Strategy: Co-located with user_id (same shard as users)
         * Compliance: HIPAA PHI protection, consent-based access
         */
        Schema::create('patients', function (Blueprint $table) {
            $table->id();
            $table->uuid('patient_uuid')->unique()->index()->comment('Facility-facing public ID');
            $table->unsignedBigInteger('user_id')->unique();
            
            // Medical record identification
            $table->string('medical_record_number_hash', 128)->unique()->comment('Hospital MRN hash');
            $table->string('medical_record_number_encrypted', 512);
            $table->string('previous_mrn_list_encrypted', 2048)->nullable()->comment('For merged records');
            
            // Demographics (critical for clinical decisions)
            $table->date('date_of_birth')->index();
            $table->enum('biological_sex', ['male', 'female', 'intersex', 'unknown'])->comment('Clinical sex for medical decisions');
            $table->enum('gender_identity', ['male', 'female', 'non_binary', 'prefer_not_to_say', 'other'])->nullable();
            $table->string('blood_type', 5)->nullable()->index();
            $table->string('ethnicity', 100)->nullable();
            $table->json('genetic_markers')->nullable()->comment('Relevant genetic information');
            
            // Emergency contacts (encrypted JSONB structure)
            $table->json('emergency_contact_chain_encrypted')->nullable()->comment('Prioritized list of emergency contacts');
            
            // Clinical flags
            $table->json('known_allergies')->nullable()->comment('Drug, food, environmental allergies');
            $table->json('chronic_conditions')->nullable()->comment('ICD-10 codes for chronic diseases');
            $table->json('active_medications')->nullable()->comment('Current medication list');
            $table->boolean('is_organ_donor')->default(false);
            $table->json('advance_directives')->nullable()->comment('DNR, living will, etc.');
            
            // Risk stratification
            $table->unsignedTinyInteger('acuity_baseline')->default(1)->comment('1=Low, 5=Critical');
            $table->json('risk_factors')->nullable()->comment('Fall risk, infection risk, etc.');
            $table->boolean('requires_isolation')->default(false);
            $table->string('isolation_type', 50)->nullable()->comment('Contact, droplet, airborne');
            
            // Consent & privacy
            $table->enum('default_consent_level', ['full', 'restricted', 'minimal', 'none'])->default('full');
            $table->json('privacy_flags')->comment('GDPR: right_to_erasure_requested, data_portability, etc.');
            $table->boolean('research_participation_allowed')->default(false);
            $table->boolean('data_sharing_allowed')->default(false);
            
            // Insurance & financial
            $table->string('primary_insurance_provider', 200)->nullable();
            $table->string('primary_insurance_id_encrypted', 512)->nullable();
            $table->string('secondary_insurance_provider', 200)->nullable();
            $table->string('secondary_insurance_id_encrypted', 512)->nullable();
            $table->enum('payment_responsibility', ['self_pay', 'insurance', 'government', 'charity'])->default('self_pay');
            
            // Care coordination
            $table->unsignedBigInteger('primary_care_provider_staff_id')->nullable();
            $table->unsignedBigInteger('primary_care_facility_id')->nullable();
            $table->timestamp('last_wellness_visit_at')->nullable();
            $table->timestamp('next_scheduled_appointment_at')->nullable();
            
            // Patient portal
            $table->boolean('portal_access_enabled')->default(true);
            $table->timestamp('portal_terms_accepted_at')->nullable();
            $table->string('preferred_language', 10)->default('en');
            $table->string('preferred_communication_method', 20)->default('email')->comment('email, sms, phone, postal');
            
            // Status tracking
            $table->enum('status', ['active', 'inactive', 'deceased', 'merged', 'test_patient'])->default('active')->index();
            $table->timestamp('deceased_at')->nullable();
            $table->unsignedBigInteger('merged_into_patient_id')->nullable()->comment('If duplicate record');
            
            // Audit trail
            $table->timestamps();
            $table->softDeletes();
            $table->unsignedBigInteger('created_by_staff_id')->nullable();
            $table->unsignedBigInteger('updated_by_staff_id')->nullable();
            $table->json('metadata')->nullable();
            
            // Foreign keys
            $table->foreign('user_id')->references('id')->on('users')->onDelete('cascade');
            
            // Performance indexes
            $table->index(['status', 'primary_care_facility_id']);
            $table->index(['date_of_birth', 'biological_sex']);
            $table->index(['last_wellness_visit_at', 'status']);
            $table->index('primary_care_provider_staff_id');
        });